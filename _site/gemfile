source 'https://rubygems.org'

gem 'jekyll', '3.7.4'

group :jekyll_plugins do
  gem 'jekyll-seo-tag', '2.4.0'
  gem 'jekyll-archives'
  gem 'jekyll-sitemap', '1.2.0'
end

# note that jekyll-sitemap plugin must be listed AFTER plugins which would generate pages to be listed in sitemap
